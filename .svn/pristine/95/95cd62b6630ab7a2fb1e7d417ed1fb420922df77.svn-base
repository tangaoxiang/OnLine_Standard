using System;
using System.Collections.Generic;
using System.Text;
using System.Web.UI.WebControls;
using System.Data;

namespace DigiPower.Onlinecol.Standard.CBLL
{
    public class UserManage
    {
        public UserManage()
        { }

        /// <summary>
        /// 设置列表值
        /// </summary>
        /// <param name="DepartmentID"></param>
        //public string SetDrop(int RoleID)
        //{
        //    DigiPower.Onlinecol.Standard.BLL.UserOperate bll = new DigiPower.Onlinecol.Standard.BLL.UserOperate();

        //    //绑定角色
        //    DataSet ds = bll.GetDepartmentList("select RoleID from T_Role where RoleID =" + RoleID + "");

        //    if (ds != null && ds.Tables.Count > 0 && ds.Tables[0].Rows.Count > 0)
        //    {
        //        return ds.Tables[0].Rows[0][0].ToString();
        //    }
        //    else
        //    {
        //        return null;
        //    }
        //}

        /// <summary>
        /// 绑定列表
        /// </summary>
        /// <param name="drp"></param>
        /// <param name="SqlWhere"></param>
        //public void BindDrop(DropDownList drp, string SqlWhere)
        //{
        //    DigiPower.Onlinecol.Standard.BLL.UserOperate bll = new DigiPower.Onlinecol.Standard.BLL.UserOperate();

        //    //绑定角色
        //    DataSet ds = bll.GetDepartmentList("select RoleID,RoleName from T_Role where " + SqlWhere + "");

        //    if (ds != null && ds.Tables.Count > 0 && ds.Tables[0].Rows.Count > 0)
        //    {
        //        for (int i = 0; i < ds.Tables[0].Rows.Count; i++)
        //        {
        //            ListItem item = new ListItem();

        //            item.Text = ds.Tables[0].Rows[i]["RoleName"].ToString();

        //            item.Value = ds.Tables[0].Rows[i]["RoleID"].ToString();

        //            drp.Items.Add(item);
        //        }
        //    }
        //}
        //public void ChangePasswd(string UserID,string NewPasswd)
        //{
        //    DigiPower.Onlinecol.Standard.BLL.UserOperate bll = new DigiPower.Onlinecol.Standard.BLL.UserOperate();
        //    bll.ChangePasswd(UserID, NewPasswd);
        //}
    }
}
